import React from "react";
import { Outlet } from "react-router-dom";

const ApprovePage = () => {
  return (
    <React.Fragment>
      <Outlet />
    </React.Fragment>
  );
};

export default ApprovePage;
